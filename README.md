# Расчёт неопределённости методики

Одностраничное веб-приложение для лаборатории: ввод исходных данных, выбор типовых составляющих неопределённости, автоматический расчёт бюджета и оформление вывода в духе **ГОСТ 34100.3-2017** / ISO/IEC Guide 98-3:2008.

## Возможности

- Ввод реквизитов протокола и описания измеряемой величины
- Добавление составляющих неопределённости (тип A/B, распределения)
- Расчёт стандартной и расширенной неопределённости, коэффициентов чувствительности (где применимо)
- Формирование текстового протокола и печать
- Светлая и тёмная тема (кнопка в правом нижнем углу, выбор сохраняется в браузере)

## Требования

Только современный браузер с включённым JavaScript. Сервер не обязателен: достаточно открыть HTML-файл с диска или раздать его через любой статический хостинг.

## Как открыть локально

1. Клонируйте репозиторий или скачайте архив.
2. Откройте в браузере [`index.html`](./index.html) (перенаправление на основное приложение) или сразу [`Расчет неопределенности.html`](./Расчет%20неопределенности.html).

При разработке удобно использовать встроенный предпросмотр в редакторе или простой HTTP-сервер, например:

```bash
npx --yes serve .
```

затем в браузере перейти по указанному адресу и открыть нужный `.html`.

## Структура репозитория

| Файл / каталог | Назначение |
|----------------|------------|
| `Расчет неопределенности.html` | Приложение: разметка, стили, логика |
| `index.html` | Переход на основной файл (удобно для корня сайта и GitHub Pages) |
| `README.md` | Описание проекта |
| `LICENSE` | Лицензия MIT |
| `.gitignore` | Исключения для Git |
| `.gitattributes` | Правила окончаний строк и типов файлов |

## Первый коммит в Git

В каталоге проекта выполните:

```bash
git init
git add .
git commit -m "Initial commit: калькулятор неопределённости"
```

Привяжите удалённый репозиторий (`git remote add origin …`) и отправьте ветку (`git push -u origin main`).

## Публикация на GitHub Pages

Если нужен адрес вида `https://<user>.github.io/<repo>/`:

1. В настройках репозитория включите **Pages** с веткой `main` и папкой `/ (root)`.
2. Сайт откроется с корня (`index.html` уже перенаправляет на основной файл).

При желании можно переименовать основной HTML в латиницу (например `app.html`) и поправить ссылку в `index.html`.

## Ограничение ответственности

Инструмент носит вспомогательный характер. Итоговые процедуры оценки неопределённости, оформление документов и соответствие требованиям аккредитации определяются политикой вашей лаборатории и применимыми нормами.

## Лицензия

См. файл [LICENSE](./LICENSE). При необходимости замените год и строку с правообладателем в заголовке лицензии.
